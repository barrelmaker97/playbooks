---
- name: Create cluster user
  hosts: localhost
  # roles/user/vars/main.yaml reads ansible_facts.env and .architecture.
  gather_facts: true
  gather_subset: [min]
  vars_files:
    - vars/user.yaml
  roles:
    - user
