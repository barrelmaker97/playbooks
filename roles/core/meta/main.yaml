---
galaxy_info:
  author: Nolan Cooper
  description: >-
    Deploy cluster-wide core services - storage, networking, certificates,
    monitoring and routing.
  license: GPL-3.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [all]
    - name: Ubuntu
      versions: [all]
  galaxy_tags: [kubernetes, monitoring, networking]

# helm is pulled in with include_role + tasks_from where it is needed. Listing
# it here would make Ansible run a tasks/main entry point it does not have.
dependencies: []
