---
galaxy_info:
  author: Nolan Cooper
  description: Install Helm and deploy chart releases via a diff-then-upgrade flow.
  license: GPL-3.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [all]
    - name: Ubuntu
      versions: [all]
  galaxy_tags: [helm, kubernetes]

dependencies: []
