---
galaxy_info:
  author: Nolan Cooper
  description: Deploy application namespaces, PostgreSQL clusters and Helm releases.
  license: GPL-3.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [all]
    - name: Ubuntu
      versions: [all]
  galaxy_tags: [kubernetes, helm]

# helm and cloudnative_pg are pulled in with include_role where they are
# needed. Listing helm here would make Ansible run a tasks/main entry point it
# does not have.
dependencies: []
