---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: "{{ postgresql_cluster.key }}"
  namespace: "{{ postgresql_cluster.value.namespace }}"
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: "{{ postgresql_cluster.key }}"
  podMetricsEndpoints:
    - port: metrics
