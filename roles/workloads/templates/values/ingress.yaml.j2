---
baseDomain: "{{ external_domain }}"
ingress:
  enabled: false
httpRoute:
  enabled: true
  parentRefs:
    - name: "{{ gateway_name }}"
      namespace: "{{ gateway_namespace }}"
      sectionName: https-dotfiles
